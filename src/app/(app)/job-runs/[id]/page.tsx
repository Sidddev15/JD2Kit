import { Shell } from "@/components/app/shell";

export default function JobRunDetailsPage() {
  return (
    <Shell>
      <h1 className="text-2xl font-semibold">Job Run</h1>
      <p className="mt-2 text-sm text-muted-foreground">
        Details view (Phase 4).
      </p>
    </Shell>
  );
}
